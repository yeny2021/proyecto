<%= form_with(model: proy) do |form| %>
  <% if proy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(proy.errors.count, "error") %> prohibited this proy from being saved:</h2>

      <ul>
        <% proy.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :imagen %>
    <%= form.text_field :imagen %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
