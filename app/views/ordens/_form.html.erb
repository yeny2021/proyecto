<%= form_with(model: orden) do |form| %>
  <% if orden.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(orden.errors.count, "error") %> prohibited this orden from being saved:</h2>

      <ul>
        <% orden.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cliente_id %>
    <%= form.text_field :cliente_id %>
  </div>

  <div class="field">
    <%= form.label :codigo %>
    <%= form.text_field :codigo %>
  </div>

  <div class="field">
    <%= form.label :proceso %>
    <%= form.date_select :proceso %>
  </div>

  <div class="field">
    <%= form.label :entrega %>
    <%= form.date_select :entrega %>
  </div>

  <div class="field">
    <%= form.label :cierre %>
    <%= form.date_select :cierre %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
