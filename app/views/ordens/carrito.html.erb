<h1>Carrito</h1>
<table class="table">
  <tbody>
    <% @orden.orden_producto.each do |t| %>
    <tr>
      <td><%= t.producto.nombre %></td>
      <td><%= t.cantidad %></td>
    </tr>
    <% end %>
  </tbody>
</table>
